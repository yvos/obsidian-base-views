# PLAN-20260215_Basesビュー一覧仕様調整2

## 1. 目的
- `formulas.viewListSize` を Bases 仕様に合わせて文字列保存へ変更する。
- top配置 + プロパティ表示ON時の行高を、値有無に関わらず統一する。
- view一覧右クリックメニューからプロパティ表示ON/OFFを切替可能にする。
- view一覧幅のグローバル保持を廃止し、`viewListSize` 不在時はデフォルト幅 + 自動短縮のみで動作させる。

## 2. 実装タスク
- [x] `BaseViewListYamlStore` の `viewListSize` 読み書きを文字列前提へ変更（処理時は数値化）
- [x] `BasesViewListSidebarService` の幅決定ロジックから `basesViewListWidthPx` 依存を除去
- [x] top配置 + プロパティ表示ON時に全view行を2行高へ統一（空値は空行プレースホルダ）
- [x] 右クリックメニューに「プロパティ表示ON/OFF」トグルを追加（配置切替と共存）
- [x] i18nキー（ja/en）追加
- [x] unit test更新（YAML文字列保存、幅ロジック、右クリックトグル、top行高統一）
- [x] `LOG-20260215.md` / `IMPLEMENTATION-base_view_list.md` / `IMPLEMENTATION.md` 更新

## 3. 検証計画
- Unit:
  - `tests/unit/bases/BaseViewListYamlStore.test.ts`
  - `tests/unit/bases/BasesViewListSidebarService.test.ts`
- Manual:
  - `viewListSize` が文字列で保存されること
  - top配置時に行高が揃うこと
  - 右クリックでプロパティ表示が切替わること

## 4. 実行メモ
- 実行環境に `npm` がない場合、テスト未実行理由をログへ明記する。
