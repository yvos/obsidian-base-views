# PLAN-20260212 列幅リサイズと横スクロール

## 目的
`tasknotesCustomTable` に対して、列境界線表示・ヘッダー列リサイズ・横スクロール表示を実装し、組み込みTable viewに近い操作性を提供する。

## スコープ
- `src/bases/CustomTableView.ts` の列幅管理とドラッグ操作
- `styles/bases-views.css` の区切り線/リサイズハンドル/スクロール調整
- `tests/unit/bases/` の列幅ロジックテスト
- `AIdocs/LOG-20260212.md` と `AIdocs/IMPLEMENTATION.md` 更新

## 実装タスク
- [x] `columnSize` を `config.get/set("columnSize")` で読み書き
- [x] 列幅テンプレートを `px` 連結方式に変更
- [x] 通常描画の `colgroup` 適用
- [x] 仮想描画のヘッダー/行/summary 幅同期
- [x] ヘッダー右端ドラッグで対象列のみリサイズ
- [x] 列幅変更時のみ `columnSize` を保存
- [x] 列境界線（縦罫線）をヘッダー/セル/summaryへ適用
- [x] 横スクロール表示（列幅合計ベース）
- [x] 単体テスト追加（列幅正規化・テンプレート・合計幅）
- [ ] 検証
  - [ ] `npm run typecheck`（実行不可: この環境に npm が存在しない）
  - [ ] `npm run build`（未実行: 同上）
- [x] ドキュメント更新
  - [x] `AIdocs/LOG-20260212.md`
  - [x] `AIdocs/IMPLEMENTATION.md`

## 受け入れ条件
- 列と列の間に細い縦区切り線が表示される
- ヘッダー区切り線ドラッグで列幅を変更できる
- 変更列幅が `columnSize` 形式で保持される
- 列幅合計がビュー幅を超えた場合、横スクロールバーが表示される
