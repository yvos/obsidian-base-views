# PLAN-20260212 仮想スクロール段階的導入

## 目的
`tasknotesCustomTable` の初回表示・ソート再描画の体感性能を改善するため、仮想スクロールを段階導入する。

## スコープ
- Phase 1: ungrouped 仮想化（自動閾値）
- Phase 2: grouped 仮想化（自動閾値）
- 既存機能（summary, file.nameリンク, rowHeight）の維持

## 実装タスク
- [x] `src/bases/CustomTableView.ts` に仮想スクロール分岐を実装（ungrouped / grouped）
- [x] `src/bases/customTableVirtualization.ts` に閾値判定・groupedフラット化ロジックを実装
- [x] `styles/bases-views.css` に仮想行/仮想ヘッダー/仮想summary向けスタイルを追加
- [x] `tests/unit/bases/customTableVirtualization.test.ts` を追加
- [x] `tests/unit/bases/customTableGroupedFlatten.test.ts` を追加
- [ ] 検証
  - [ ] `npm run typecheck`（実行不可: この環境に npm が存在しない）
  - [ ] `npm run build`（未実行: typecheckと同理由）
- [x] ドキュメント更新
  - [x] `AIdocs/LOG-20260212.md`
  - [x] `AIdocs/IMPLEMENTATION.md`

## 受け入れ条件
- ungrouped で閾値以上のとき仮想描画が有効になる
- grouped で flatten 件数が閾値以上のとき仮想描画が有効になる
- 小規模データでは通常描画を維持する
- summary / file.nameリンク / rowHeight が維持される
