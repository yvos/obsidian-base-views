# PLAN-20260212 デバウンス最適化

## 目的
Custom Table View (`tasknotesCustomTable`) の初回表示とソート・表示設定切替時の待機時間を短縮する。

## スコープ
- `src/bases/CustomTableView.ts` の更新タイミング最適化
- `BasesViewBase` には手を入れず、影響範囲を Custom Table View に限定

## タスク
- [x] `onDataUpdated()` を CustomTableView でオーバーライド
- [x] 初回データ更新時は即時描画
- [x] sort/order/group/options 変化を署名比較で検出し即時描画
- [x] 通常データ更新のみ短デバウンス（120ms）
- [ ] 検証（typecheck/build）
  - [ ] `npm run typecheck`（実行不可: Node/npm がこの環境に存在しない）
  - [ ] `npm run build`（実行不可: Node/npm がこの環境に存在しない）
- [x] ドキュメント更新
  - [x] `AIdocs/LOG-20260212.md`
  - [x] `AIdocs/IMPLEMENTATION.md`

## 受け入れ条件
- 初回表示の遅延が 500ms 固定待ちにならない
- sort/order 変更の反映が体感即時になる
- 頻繁なファイル更新時は再描画連打を抑制できる
